name: Auth Service CI

on:
  push:
    branches:
      - main
      - dev
  pull_request:
    branches:
      - main
      - dev
  workflow_dispatch:
    inputs:
      release_tag:
        description: "Override release tag (default: commit SHA)"
        required: false
        default: ""

jobs:
  ci:
    uses: Nimesh-Capstone-Project/Parking-System/.github/workflows/reusable-ci.yaml@master
    with:
      release_tag: ${{ github.event.inputs.release_tag || github.sha }}
      image_name: auth-service
      dockerfile_path: .
      node_version: "20"
      sonar_key: "Nimesh-Capstone-Project_Parking-System-AuthService_38163bf1-a4fc-44fd-8116-772f4ea206f6"
      snyk_severity_threshold: "high"
    secrets: inherit